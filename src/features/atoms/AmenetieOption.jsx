import React from "react";

const AmenetieOption = ({ name }) => {
  return (
    <label htmlFor="" style={{ display: "inline-flex", fontSize: "12px" }}>
      <input type="checkbox" name="" id="" />
      &nbsp; {name}
    </label>
  );
};

export default AmenetieOption;
